<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- One app, every role's screens ship inside it: these permissions are
         the real union every role genuinely needs (farmer geolocation/camera,
         admin goods-receipt camera capture), not a "trim later" placeholder. -->

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location for Farm Geolocation & Boundary Mapping (BR-33 / Prominent Disclosure) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Camera & Storage for Crop & Certification Uploads, and (per consolidation
         plan §6) Admin's goods-receipt camera capture -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MainApplication"
        android:label="TOHFA Mobile"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- PLACEHOLDER — Facebook Login (farmer OAuth). @string/facebook_app_id
             and @string/facebook_client_token in res/values/strings.xml must be
             replaced with real values from the Facebook Developer Console
             before this works; see that file's comment. Google Sign-In needs
             no manifest entry (Play Services auto-linking handles it) — its
             web client ID is configured at runtime in
             roles/farmer/api/googleSignIn.ts, also a placeholder today.

             AutoInitEnabled/AutoLogAppEventsEnabled are off on purpose: with
             a placeholder App ID, the SDK's default auto-init tries to log
             an app-launch event on every cold start and fails loudly
             (com.facebook.GraphResponse "Invalid application ID", plus a
             NativeEventEmitter addListener/removeListeners warning from its
             analytics bridge) before the farmer has ever touched the
             Facebook button. Flip both back to true once a real App ID is
             in place, if app-events logging is actually wanted. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <meta-data
            android:name="com.facebook.sdk.AutoInitEnabled"
            android:value="false" />
        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="false" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="TOHFA Mobile"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tohfa" />
            </intent-filter>
        </activity>
    </application>
</manifest>
