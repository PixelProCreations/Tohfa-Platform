diff --git a/android/src/main/old-arch/com/rnmapbox/rnmbx/NativeRNMBXLocationModuleSpec.java b/android/src/main/old-arch/com/rnmapbox/rnmbx/NativeRNMBXLocationModuleSpec.java
index e5db5cbba2914c1c02c2cf33d02fe8614bc68bc7..4f2b6e0485709bf9cb2c0e39961f57f559cff1d4 100644
--- a/android/src/main/old-arch/com/rnmapbox/rnmbx/NativeRNMBXLocationModuleSpec.java
+++ b/android/src/main/old-arch/com/rnmapbox/rnmbx/NativeRNMBXLocationModuleSpec.java
@@ -13,6 +13,7 @@
 package com.rnmapbox.rnmbx;
 
 import com.facebook.proguard.annotations.DoNotStrip;
+import com.facebook.react.bridge.Callback;
 import com.facebook.react.bridge.Promise;
 import com.facebook.react.bridge.ReactApplicationContext;
 import com.facebook.react.bridge.ReactContextBaseJavaModule;
@@ -25,6 +26,17 @@ import javax.annotation.Nonnull;
 public abstract class NativeRNMBXLocationModuleSpec extends ReactContextBaseJavaModule implements ReactModuleWithSpec, TurboModule {
   public static final String NAME = "RNMBXLocationModule";
 
+  // RN >=0.76 declares this on BaseJavaModule/ReactContextBaseJavaModule itself
+  // (facebook/react-native#44906) so TurboModule event emission works without a
+  // vendored field. This project is on RN 0.74.5 with newArchEnabled=false, where
+  // the field doesn't exist upstream, so @rnmapbox/maps@10.2.9's RNMBXLocationModule.kt
+  // (which unconditionally references it) fails to compile. Declaring it here, always
+  // null under old architecture, makes that reference resolve and fall through to the
+  // module's existing old-arch EventEmitter.getModuleEmitter(...) path — the correct
+  // behavior for this build, not a functional change. See
+  // https://github.com/rnmapbox/maps/issues/3795.
+  protected Callback mEventEmitterCallback;
+
   public NativeRNMBXLocationModuleSpec(ReactApplicationContext reactContext) {
     super(reactContext);
   }
